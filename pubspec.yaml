name: crud_http_client

description: Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^0.12.2
  http: ^0.12.0
  url_launcher: any
  flutter_launcher_icons: ^0.7.0
  oauth2: ^1.5.0
  shared_preferences:
  bloc: ^0.15.0
  flutter_bloc: ^0.21.0
  equatable: ^0.5.1
  date_format: ^1.0.8
  intl: ^0.16.1
  flutter_local_notifications:
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
